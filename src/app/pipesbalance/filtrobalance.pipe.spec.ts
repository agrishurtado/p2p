import { FiltrobalancePipe } from './filtrobalance.pipe';

describe('FiltrobalancePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrobalancePipe();
    expect(pipe).toBeTruthy();
  });
});
